#include <stdio.h>
/** to write a code that prints letters of the alphabet in reverse
 */

int main(void)
{
char alphabet
	;
for (alphabet = 'z'; alphabet >= 'a'; alphabet--)

putchar(alphabet);

putchar('\n');
return(0);
}
